#include "XSFML.h"

void XSFML::parse(const char* data)
{
	bool isEndLine = false;

	std::vector<Token> tokens;

	unsigned int index = 0;

	while (not isEndLine) {
		if (data[index] == '\0') isEndLine = true;


	}
}
